德州帳本 最終版

包含：Google 永久房主、其他人唯讀、Firebase 永久保存、月年統計、即時同步、PWA 主畫面。

更新 GitHub：刪除舊的 index.html / styles.css / app.js / sw.js / manifest.webmanifest，再上傳本壓縮檔中的同名檔案。

Firebase：把 firestore.rules 的內容貼到 Firestore 規則並發布。

iPhone：Safari 開啟網站 → 分享 → 加入主畫面。